# npm configuration
# Note: Peer dependency warnings about React versions are expected and harmless.
# The @radix-ui packages (dependencies of @excalidraw/excalidraw) haven't
# updated their peer dependencies to include React 19 yet, but they work fine.
# The 'overrides' field in package.json ensures React 19 is used consistently.
fund=false

